[/Script/ModelContextProtocolEngine.ModelContextProtocolSettings]
bAutoStartServer=True
ServerPortNumber=8000
ServerUrlPath=/mcp
bEnableToolSearch=True

[/Script/UnrealEd.EditorLoadingSavingSettings]
; Editor autosave OFF: the tool's build operations constantly dirty the scratch
; Workspace level (thousands of transient proc-mesh actors), so autosave kept
; locking the editor to save a map that holds nothing worth saving. Real work
; is saved by the tool itself (session files + spatial exports).
bAutoSaveEnable=False
